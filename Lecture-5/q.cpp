#include<iostream>
using namespace std;

int main(){
    int a,b,sum;
    
    cout<<"Enter the number of a : ";
    cin >> a;
    cout<<"Enter the number of b : ";
    cin >> b;

    sum = a + b;
    cout<<"sum of a and b is : "<<sum<<endl;

}